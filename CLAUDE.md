# 챌룸(ChallRoom) 프로젝트

## 프로젝트 개요

인기 챌린지 영상(춤, 운동 등)을 발견하고, 친구들을 초대해 "방"을 만들어 같은 챌린지를 따라 찍어 공유하는 서비스.

**시나리오**: 인기 챌린지 발견 → 방 생성 및 초대 → 친구들이 따라 찍어 업로드 → 같은 방 안에서 공유.

## 기술 스택

- **백엔드**: FastAPI (Python), 비동기 우선
- **DB**: PostgreSQL (배열/JSONB 필드, 다형성 관계 쿼리 이점 때문에 MariaDB 대신 선택)
- **비동기 작업**: Celery 또는 RQ (영상 업로드 후처리, 웹훅 콜백 처리용)
- **프론트엔드**: Flutter


프론트 코드 구조와 컨벤션은
`.claude/skills/flutter/SKILL.md`에 있다.

